// getters
const navbarMenu = state => state.navbarMenu

const roomByUser = state => state.roomByUser

const navbarAzure = state => state.navbarAzure

const i18nReady = state => state.i18nReady

export default {
  navbarMenu,
  roomByUser,
  navbarAzure,
  i18nReady
}
